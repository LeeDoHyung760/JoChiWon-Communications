import { useEffect,useState,type MouseEvent } from 'react';
import { MapPin,Sparkles,X } from 'lucide-react';
import type { DirectMessage,DirectRecommendationPlace,DirectRoom,DirectRoomMeetingPlace } from '../../shared/socket-events';
import { API_BASE_URL } from '../config/api';
import { socket } from '../game/systems/socketClient';

const allowedHosts=['place.map.kakao.com','map.kakao.com','kko.to'];
const mapUrl=(place:{name:string;address:string;externalUrl?:string})=>{const external=place.externalUrl?.trim();if(external)return external;return place.name&&place.address?`https://map.kakao.com/?q=${encodeURIComponent(`${place.name} ${place.address}`)}`:''};
export function openKakaoMap(event:MouseEvent<HTMLButtonElement>,place:{name:string;address:string;externalUrl?:string},showToast:(message:string)=>void){event.preventDefault();event.stopPropagation();const value=mapUrl(place);if(!value)return showToast('카카오맵 링크를 찾을 수 없습니다.');let parsed:URL;try{parsed=new URL(value)}catch{return showToast('올바르지 않은 카카오맵 링크입니다.')}if(!allowedHosts.some(host=>parsed.hostname===host||parsed.hostname.endsWith(`.${host}`)))return showToast('허용되지 않은 지도 링크입니다.');const opened=window.open(parsed.toString(),'_blank','noopener,noreferrer');if(!opened)showToast('팝업이 차단되었습니다. 브라우저에서 팝업을 허용해 주세요.')}

export function DirectRecommendationControls({room,messageCount}:{room:DirectRoom;messageCount:number}){
 const [consentOpen,setConsentOpen]=useState(false),[loading,setLoading]=useState(false),[stage,setStage]=useState(''),[error,setError]=useState(''),[userRequest,setUserRequest]=useState('');
 useEffect(()=>{const started=(data:{directRoomId:string;stage:'analyzing'|'searching'})=>{if(data.directRoomId!==room.id)return;setLoading(true);setStage(data.stage==='analyzing'?'충녕이가 두 분의 대화를 살펴보고 있어요':'조치원의 실제 장소를 찾고 있어요')};const completed=(data:{directRoomId:string})=>{if(data.directRoomId!==room.id)return;setLoading(false);setStage('');setError('')};const failed=(data:{directRoomId:string;message:string})=>{if(data.directRoomId!==room.id)return;setLoading(false);setStage('');setError(data.message)};socket.on('directRecommendationStarted',started);socket.on('directRecommendationCompleted',completed);socket.on('directRecommendationFailed',failed);return()=>{socket.off('directRecommendationStarted',started);socket.off('directRecommendationCompleted',completed);socket.off('directRecommendationFailed',failed)}},[room.id]);
 const request=async()=>{setConsentOpen(false);setLoading(true);setError('');try{const response=await fetch(`${API_BASE_URL}/direct-rooms/${encodeURIComponent(room.id)}/recommendations`,{method:'POST',headers:{'Content-Type':'application/json','X-Socket-Id':socket.id??''},body:JSON.stringify(userRequest.trim()?{userRequest:userRequest.trim()}: {})});if(!response.ok){const body=await response.json() as {error?:string};throw new Error(body.error??'추천 요청에 실패했어요.')}}catch(error){setLoading(false);setStage('');setError(error instanceof Error?error.message:'네트워크 오류가 발생했어요.')}};
 const enabled=room.active&&room.participants.some(participant=>participant.id===socket.id)&&messageCount>=2&&!loading;
 return <div className="direct-recommendation-controls"><button type="button" disabled={!enabled} onClick={()=>setConsentOpen(true)}><Sparkles size={15}/> 충녕이에게 장소 추천받기</button>{messageCount<2&&<small>두 분의 대화를 조금 더 나눈 뒤 추천받아 보세요.</small>}{stage&&<small>{stage}</small>}{error&&<small className="error">{error}</small>}{consentOpen&&<div className="recommendation-overlay"><section className="recommendation-modal"><button type="button" className="close" onClick={()=>setConsentOpen(false)}><X/></button><h2>대화 분석에 동의하시나요?</h2><p>충녕이가 이 채팅방의 최근 대화 최대 20개와 두 사용자의 공개 관심사를 분석하여 함께 방문하기 좋은 장소를 추천합니다.</p><input value={userRequest} maxLength={300} onChange={event=>setUserRequest(event.target.value)} placeholder="추가 조건이 있다면 입력해 주세요 (선택)"/><div><button type="button" onClick={()=>setConsentOpen(false)}>취소</button><button type="button" className="primary" onClick={()=>void request()}>동의하고 추천받기</button></div></section></div>}</div>
}

export function DirectRecommendationMessage({message,room,showToast}:{message:DirectMessage;room:DirectRoom;showToast:(message:string)=>void}){
 const recommendation=message.recommendation,[selected,setSelected]=useState<DirectRecommendationPlace|null>(null),[saving,setSaving]=useState(false);if(!recommendation)return null;
 const save=async()=>{if(!selected)return;setSaving(true);try{const response=await fetch(`${API_BASE_URL}/direct-rooms/${encodeURIComponent(room.id)}/meeting-place`,{method:'PUT',headers:{'Content-Type':'application/json','X-Socket-Id':socket.id??''},body:JSON.stringify({recommendationId:recommendation.recommendationId,placeId:selected.id})});const body=await response.json() as {error?:string;changed?:boolean};if(!response.ok)throw new Error(body.error??'모임 장소를 등록하지 못했습니다. 다시 시도해 주세요.');showToast(body.changed?'모임 장소가 변경되었습니다.':'모임 장소로 등록되었습니다.');setSelected(null)}catch(error){showToast(error instanceof Error?error.message:'모임 장소를 등록하지 못했습니다. 다시 시도해 주세요.')}finally{setSaving(false)}};
 return <article className="direct-ai-message"><b>[충녕이]</b><p>{recommendation.summary}</p>{recommendation.places.map(place=><section className="place-card recommendation-card" key={place.id}><small>{place.category}</small><h3>{place.name}</h3><p><MapPin size={14}/>{place.roadAddress||place.address}</p><p>{place.recommendationReason}</p><div><button type="button" disabled={!mapUrl(place)} title={mapUrl(place)?undefined:'지도 링크가 제공되지 않은 장소입니다.'} onClick={event=>openKakaoMap(event,place,showToast)}>카카오맵에서 보기</button><button type="button" onClick={event=>{event.preventDefault();event.stopPropagation();setSelected(place)}}>모임 장소로 선택</button></div></section>)}{selected&&<div className="recommendation-overlay"><section className="recommendation-modal meeting-confirm"><button type="button" className="close" onClick={()=>setSelected(null)}><X/></button><h2>{room.meetingPlace?'기존 모임 장소를 이 장소로 변경할까요?':'이 장소를 모임 장소로 등록할까요?'}</h2><h3>{selected.name}</h3><p>{selected.category}</p><p>{selected.roadAddress||selected.address}</p><p>등록하면 두 채팅 참여자에게 공지됩니다.</p><div><button type="button" onClick={()=>setSelected(null)}>취소</button><button type="button" className="primary" disabled={saving} onClick={()=>void save()}>{saving?'등록 중...':'모임 장소로 등록'}</button></div></section></div>}</article>
}

export function MeetingPlaceBanner({room,showToast}:{room:DirectRoom;showToast:(message:string)=>void}){const place=room.meetingPlace;const remove=async()=>{try{const response=await fetch(`${API_BASE_URL}/direct-rooms/${encodeURIComponent(room.id)}/meeting-place`,{method:'DELETE',headers:{'X-Socket-Id':socket.id??''}});const body=await response.json() as {error?:string};if(!response.ok)throw new Error(body.error??'모임 장소 등록을 해제하지 못했습니다.');showToast('모임 장소 등록이 해제되었습니다.')}catch(error){showToast(error instanceof Error?error.message:'모임 장소 등록을 해제하지 못했습니다.')}};return <aside className="meeting-place-banner">{place?<><b>모임 장소 · {place.placeName}</b><small>{place.roadAddress||place.address}</small><small>{place.selectedByNickname} · {new Date(place.selectedAt).toLocaleString('ko-KR')}</small><div><button type="button" onClick={event=>openKakaoMap(event,{name:place.placeName,address:place.address,externalUrl:place.externalUrl},showToast)}>카카오맵 보기</button><button type="button" onClick={()=>void remove()}>등록 해제</button></div></>:<small>아직 등록된 모임 장소가 없습니다.</small>}</aside>}

export function MeetingPlaceSystemMessage({message,showToast}:{message:DirectMessage;showToast:(message:string)=>void}){const place=message.meetingPlace;return <article className="meeting-place-system"><b>[공지]</b><p>{message.message}</p>{place&&<><strong>{place.placeName}</strong><small>{place.roadAddress||place.address}</small><button type="button" onClick={event=>openKakaoMap(event,{name:place.placeName,address:place.address,externalUrl:place.externalUrl},showToast)}>카카오맵에서 보기</button></>}</article>}
