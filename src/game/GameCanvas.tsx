import { useEffect,useRef } from 'react';import Phaser from 'phaser';import { WorldScene } from './scenes/WorldScene';import { gameEvents } from './events';import { socket } from './systems/socketClient';import type { UserProfile } from '../types';
export function GameCanvas({profile}:{profile:UserProfile}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const enrich=()=>{socket.emit('joinMap',{mapId:'town',nickname:profile.nickname,appearance:profile.character,matchProfile:{mbti:profile.mbti,interests:profile.interests,usagePurposes:profile.usagePurposes,preferredPlaceCategories:profile.preferredPlaceCategories},x:1200,y:1050})};socket.once('currentMapUsers',enrich);const game=new Phaser.Game({type:Phaser.AUTO,parent:ref.current,width:1100,height:700,backgroundColor:'#dfece7',physics:{default:'arcade'},scale:{mode:Phaser.Scale.RESIZE,autoCenter:Phaser.Scale.CENTER_BOTH}});game.scene.add('world',WorldScene,true,{profile});return()=>{socket.off('currentMapUsers',enrich);gameEvents.removeAllListeners('show-bubble');game.destroy(true)}},[profile]);return <div className="game-canvas" ref={ref}/>}
