import { useEffect,useRef } from 'react';import Phaser from 'phaser';import { WorldScene } from './scenes/WorldScene';import { gameEvents } from './events';import type { UserProfile } from '../types';
export function GameCanvas({profile}:{profile:UserProfile}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const game=new Phaser.Game({type:Phaser.AUTO,parent:ref.current,width:1100,height:700,backgroundColor:'#dfece7',physics:{default:'arcade'},scale:{mode:Phaser.Scale.RESIZE,autoCenter:Phaser.Scale.CENTER_BOTH}});game.scene.add('world',WorldScene,true,{profile});return()=>{gameEvents.removeAllListeners('show-bubble');game.destroy(true)}},[profile]);return <div className="game-canvas" ref={ref}/>}
